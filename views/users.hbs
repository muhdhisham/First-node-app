<h1>Users</h1>
{{#if hasUsers}}
    <ul>
        {{#each users }}
        <h1>No Users Found!</h1>
            <li>{{ this.name }}</li>
        {{/each}}
    </ul>
{{ else }}
    <h1>No Users Found!</h1>
{{/if}}
